import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('getVariable', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement getVariable test logic
  });
});
