import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('getResourceContent', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement getResourceContent test logic
  });
});
