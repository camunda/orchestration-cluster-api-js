import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('searchRolesForTenant', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement searchRolesForTenant test logic
  });
});
