import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('getStartProcessForm', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement getStartProcessForm test logic
  });
});
