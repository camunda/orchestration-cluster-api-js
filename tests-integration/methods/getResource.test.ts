import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('getResource', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement getResource test logic
  });
});
