import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('activateJobs', () => {
  it('scaffold', async () => {
    const camunda = createCamundaClient(); /* TODO */
  });
})