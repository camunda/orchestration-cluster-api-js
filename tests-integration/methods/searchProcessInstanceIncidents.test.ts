import { describe, it } from 'vitest';
import { createCamundaClient } from '../../dist';

describe('searchProcessInstanceIncidents', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement searchProcessInstanceIncidents test logic
  });
});
