import { describe, it } from 'vitest';
import createCamundaClient from '../../../sdks/typescript-codegen/dist';

describe('searchGroups', () => {
  it('scaffold', () => {
    const camunda = createCamundaClient();
    // TODO: implement searchGroups test logic
  });
});
