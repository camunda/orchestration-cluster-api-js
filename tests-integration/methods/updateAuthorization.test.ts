import { describe, it } from 'vitest';

import { createCamundaClient } from '../../dist';

describe('updateAuthorization', () => {
  it('scaffold', () => {
    const _camunda = createCamundaClient();
    // TODO: implement updateAuthorization test logic
  });
});
